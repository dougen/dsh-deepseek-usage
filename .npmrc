# npm reads only the .npmrc in the project root it selects -- the cwd's nearest
# ancestor holding a package.json -- so this project needs its own copy: the
# workspace-root .npmrc is not consulted from here. Absolute, because npm
# resolves a relative `cache` against this directory.
cache=D:\dsh-playground\.npm-cache
